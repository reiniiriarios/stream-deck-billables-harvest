// Rename to settings.ts, fill in data for tests.
// This data will come from Stream Deck in production.

import { Settings } from '../src/types';

const testSettings: Settings = {
  harvestAccountToken: '',
  harvestAccountId: '',
  forecastAccountId: '',
};

export default testSettings;
